node_modules/
dist/
debug/

# The local master asset kit. Large, full of authoring intermediates and
# unused variants. Only the assets the game actually references are exported
# into public/, and public/ is what ships.
source/

# Local Blender working files and automatically-created backup files.
*.blend
*.blend1
**/__pycache__/
*.pyc
